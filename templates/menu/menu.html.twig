<ul class="sidebar navbar-nav">

    {% for menu in menus %}

    <li class="nav-item">
        <a class="nav-link" href="{{ url }}{{ menu.url }}">
            <i class="fas fa-fw {{ menu.icon }}"></i>
            <span>{{ menu.name}}</span>
        </a>
    </li>


    {% endfor %}
</ul>